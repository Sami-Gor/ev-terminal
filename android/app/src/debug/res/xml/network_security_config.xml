<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only network security posture (Android Studio / emulator / adb reverse).
  Cleartext stays disabled by default and is permitted only for local
  development hosts: the emulator host loopback (10.0.2.2), localhost and
  127.0.0.1. Release builds never see this file.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
